# This file is required to treat the directory as a Python package.
# It can also be used to define what should be imported when the package is imported.

# Import module to set UMR template as default
#import umr_plot_template
import plotly.graph_objects as go
